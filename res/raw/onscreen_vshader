attribute vec2 aPosition;
uniform vec2 ratios;

varying vec2 vTextureCoord;

void main(){
	gl_Position = vec4(aPosition, 0.0, 1.0);
	//gl_Position.xy *= vec2(960./720., 720./1280.);
	gl_Position.xy *= ratios;
	vTextureCoord = (aPosition + 1.0) * 0.5;
}